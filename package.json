{
  "name": "smd-gui",
  "version": "0.2.0",
  "description": "",
  "main": "./src/main/main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vortique",
  "license": "MIT",
  "build": {
    "extraResources": [
      {
        "from": "binaries/",
        "to": "binaries",
        "filter": "**/*"
      }
    ]
  },
  "type": "module",
  "devDependencies": {
    "electron": "^39.1.1"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
